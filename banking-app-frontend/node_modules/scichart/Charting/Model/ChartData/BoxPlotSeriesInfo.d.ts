import { HitTestInfo } from "../../Visuals/RenderableSeries/HitTest/HitTestInfo";
import { IRenderableSeries } from "../../Visuals/RenderableSeries/IRenderableSeries";
import { SeriesInfo } from "./SeriesInfo";
export declare class BoxPlotSeriesInfo extends SeriesInfo {
    max: number;
    upper: number;
    median: number;
    lower: number;
    minimum: number;
    constructor(renderableSeries: IRenderableSeries, hitTestInfo: HitTestInfo);
    get formattedMaxValue(): string;
    get formattedUpperValue(): string;
    get formattedMedianValue(): string;
    get formattedLowerValue(): string;
    get formattedMinimumValue(): string;
    getValuesWithLabels(title: string | undefined, xLabel: string, yLabel: string, isXYGrouped: boolean): string[];
}

import { ETriangleSeriesDrawMode } from "../../../types/TriangleSeriesDrawMode";
import { HitTestInfo } from "../../Visuals/RenderableSeries/HitTest/HitTestInfo";
import { IRenderableSeries } from "../../Visuals/RenderableSeries/IRenderableSeries";
import { SeriesInfo } from "./SeriesInfo";
export declare class TriangleSeriesInfo extends SeriesInfo {
    xValues: number[];
    yValues: number[];
    drawMode: ETriangleSeriesDrawMode;
    vertices: number;
    constructor(renderableSeries: IRenderableSeries, hitTestInfo: HitTestInfo, drawMode: ETriangleSeriesDrawMode, polygonVertices: number);
    get formattedXValues(): string[];
    get formattedYValues(): string[];
    getValuesWithLabels(title: string | undefined, xLabel: string, yLabel: string, isXYGrouped: boolean): string[];
}

import { NumberRange } from "../../Core/NumberRange";
import { NumberArray } from "../../types/NumberArray";
import { ESearchMode } from "../../types/SearchMode";
import { SCRTDoubleVector, TSciChart } from "../../types/TSciChart";
import { BaseDataSeries, IBaseDataSeriesOptions } from "./BaseDataSeries";
import { EDataSeriesType, EDataSeriesValueType } from "./IDataSeries";
import { IPointMetadata } from "./IPointMetadata";
/**
 * Options to pass to the {@link XyxyDataSeries} constructor
 */
export interface IXyxyDataSeriesOptions extends IBaseDataSeriesOptions {
    /**
     * The X-values array to pre-populate the {@link XyxyDataSeries}
     */
    xValues?: NumberArray;
    /**
     * The Y-values array to pre-populate the {@link XyxyDataSeries}
     */
    yValues?: NumberArray;
    /**
     * The X1-values array to pre-populate the {@link XyxyDataSeries}
     */
    x1Values?: NumberArray;
    /**
     * The Y1-values array to pre-populate the {@link XyxyDataSeries}
     */
    y1Values?: NumberArray;
}
/**
 * XyxyDataSeries is a DataSeries for holding X, Y, X1, Y1 data in SciChart's 2D
 * {@link https://www.scichart.com/javascript-chart-features | JavaScript Charts}
 * @remarks
 * The {@link XyxyDataSeries} is primarily used with our {@link FastBoxRenderableSeries | JavaScript Box Chart},
 * which draws arbitrary rectangles
 *
 * A DataSeries stores the data to render. This is independent from the {@link IRenderableSeries | RenderableSeries}
 * which defines how that data should be rendered.
 *
 * See derived types of {@link BaseDataSeries} to find out what data-series are available.
 * See derived types of {@link IRenderableSeries} to find out what 2D JavaScript Chart types are available.
 *
 * ---
 * 📚 Docs: {@link https://www.scichart.com/documentation/js/v4/2d-charts/chart-types/data-series-api/data-series-api-overview/}
 */
export declare class XyxyDataSeries extends BaseDataSeries {
    /** @inheritDoc */
    readonly type: EDataSeriesType;
    /**
     * Creates an instance of {@link XyxyDataSeries}
     * @param webAssemblyContext the {@link TSciChart | SciChart WebAssembly Context} containing native methods
     * and access to our underlying WebGL2 rendering engine
     * @param options the {@link IXyzDataSeriesOptions} which can be passed to configure the DataSeries at construct time
     *
     * ---
     * 📚 Docs: {@link https://www.scichart.com/documentation/js/v4/2d-charts/chart-types/data-series-api/data-series-api-overview/}
     */
    constructor(webAssemblyContext: TSciChart, options?: IXyxyDataSeriesOptions);
    get yValues(): SCRTDoubleVector;
    get x1Values(): SCRTDoubleVector;
    get y1Values(): SCRTDoubleVector;
    /**
     * Appends a single X, Y, X1, Y1 point to the DataSeries
     * @remarks
     * For best performance on drawing large datasets, use the {@link appendRange} method
     *
     * Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param x The X-value
     * @param y The Y-value
     * @param x1 The X1-value
     * @param y1 The Y1-value
     * @param metadata The point metadata
     */
    append(x: number, y: number, x1: number, y1: number, metadata?: IPointMetadata): void;
    /**
     * Appends a range of X, Y, X1, Y1 points to the DataSeries
     * @remarks
     * This method is considerably higher performance than {@link append} which appends a single point
     *
     * Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param xValues The X-values
     * @param yValues The Y-values
     * @param x1Values The X1-values
     * @param y1Values The Y1-values
     * @param metadata The array of point metadata
     */
    appendRange(xValues: NumberArray, yValues: NumberArray, x1Values: NumberArray, y1Values: NumberArray, metadata?: IPointMetadata[]): void;
    /**
     * Updates a single Y, X1, Y1 value set by X-index
     * @remarks Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param index the index to update
     * @param y The new Y value
     * @param x1 The new X1-value
     * @param y1 The new Y1-value
     * @param metadata The point metadata
     */
    update(index: number, y: number, x1: number, y1: number, metadata?: IPointMetadata): void;
    /**
     * Updates a single X, Y, X1, Y1 value set by X-index. Might also need to set isSorted = false
     * @remarks Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param index the index to update
     * @param x The new X value
     * @param y The new Y value
     * @param x1 The new X1-value
     * @param y1 The new Y1-value
     * @param metadata The point metadata
     */
    updateXyz(index: number, x: number, y: number, x1: number, y1: number, metadata?: IPointMetadata): void;
    /**
     * Inserts a single X,Y,X1,Y1 value at the start index
     * @remarks
     * For best performance on drawing large datasets, use the {@link insertRange} method
     *
     * Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param startIndex the index to insert at
     * @param x the Xvalue
     * @param y the YValue
     * @param x1 The X1-value
     * @param y1 The Y1-value
     * @param metadata The point metadata
     */
    insert(startIndex: number, x: number, y: number, x1: number, y1: number, metadata?: IPointMetadata): void;
    /**
     * Inserts a range of X,Y, X1, Y1 values at the startIndex
     * @remarks
     * Any changes of the DataSeries will trigger a redraw on the parent {@link SciChartSurface}
     * @param startIndex the index to insert at
     * @param xValues the XValues
     * @param yValues the YValues
     * @param x1Values The X1-values
     * @param y1Values The Y1-values
     * @param metadata The array of point metadata
     */
    insertRange(startIndex: number, xValues: NumberArray, yValues: NumberArray, x1Values: NumberArray, y1Values: NumberArray, metadata?: IPointMetadata[]): void;
    /** @inheritDoc */
    getIndicesRange(xRange: NumberRange, isCategoryData?: boolean, downSearchMode?: ESearchMode, upSearchMode?: ESearchMode): NumberRange;
    /** @inheritDoc */
    getXRange(dataSeriesValueType?: EDataSeriesValueType): NumberRange;
    /** @inheritDoc */
    protected getOptions(excludeData?: boolean): IXyxyDataSeriesOptions;
    private getXyxyValues;
}

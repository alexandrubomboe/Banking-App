import { EColumnMode, EColumnYMode } from "../../../../types/ColumnMode";
/**
 * Returns rectangle borders in pixel coordinates
 */
declare function calcRectangleBorders(columnXMode: EColumnMode, columnYMode: EColumnYMode, isXAxisFlipped: boolean, isYAxisFlipped: boolean, x: number, y: number, x1: number, y1: number, isVerticalChart: boolean): {
    xMin: number;
    xMax: number;
    yMin: number;
    yMax: number;
};
export declare const dataLabelHelpers: {
    calcRectangleBorders: typeof calcRectangleBorders;
};
export {};

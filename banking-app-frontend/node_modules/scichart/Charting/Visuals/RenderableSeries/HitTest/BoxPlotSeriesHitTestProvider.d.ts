import { FastBoxPlotRenderableSeries } from "../FastBoxPlotRenderableSeries";
import { BaseHitTestProvider } from "./BaseHitTestProvider";
import { HitTestInfo } from "./HitTestInfo";
/**
 * Hit-test provider for {@link BoxPlotSeriesHitTestProvider}. See base class {@link BaseHitTestProvider} for further info
 */
export declare class BoxPlotSeriesHitTestProvider extends BaseHitTestProvider<FastBoxPlotRenderableSeries> {
    /** @inheritDoc */
    hitTest(x: number, y: number): HitTestInfo;
    /** @inheritDoc */
    hitTestForDataPointSelectionModifier(x: number, y: number, hitTestRadius?: number): HitTestInfo;
    /** @inheritDoc */
    hitTestXSlice(x: number, y: number): HitTestInfo;
}

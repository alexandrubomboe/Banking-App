import { FastTriangleRenderableSeries } from "../FastTriangleRenderableSeries";
import { BaseHitTestProvider } from "./BaseHitTestProvider";
import { HitTestInfo } from "./HitTestInfo";
/**
 * Hit-test provider for {@link FastTriangleRenderableSeries}. See base class {@link BaseHitTestProvider} for further info
 */
export declare class TriangleSeriesHitTestProvider extends BaseHitTestProvider<FastTriangleRenderableSeries> {
    /** @inheritDoc */
    hitTest(x: number, y: number): HitTestInfo;
    /** @inheritDoc */
    hitTestForDataPointSelectionModifier(x: number, y: number): HitTestInfo;
    /** @inheritDoc */
    hitTestXSlice(x: number, y: number): HitTestInfo;
    /** @inheritDoc */
    hitTestDataPoint(x: number, y: number, hitTestRadius?: number): HitTestInfo;
}

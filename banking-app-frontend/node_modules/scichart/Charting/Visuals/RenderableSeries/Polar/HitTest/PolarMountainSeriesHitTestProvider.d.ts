import { HitTestInfo } from "../../HitTest/HitTestInfo";
import { PolarDataPointHitTestProvider } from "./PolarDataPointHitTestProvider";
/**
 * Hit-test provider for {@link PolarMountainRenderableSeries}. See base class {@link BaseHitTestProvider} for further info.
 * Does not support vertical charts
 */
export declare class PolarMountainSeriesHitTestProvider extends PolarDataPointHitTestProvider {
    /** @inheritDoc */
    hitTest(x: number, y: number): HitTestInfo;
    /** @inheritDoc */
    hitTestDataPoint(x: number, y: number, hitTestRadius?: number): HitTestInfo;
    /** @inheritDoc */
    hitTestXSlice(x: number, y: number): HitTestInfo;
    /**
     * Does not support ve
     * @param x
     * @param y
     * @returns
     */
    protected hitTestMountainBody(x: number, y: number): HitTestInfo;
}

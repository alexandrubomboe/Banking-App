import { EFillPaletteMode } from "../Charting/Model/IPaletteProvider";
export declare type TDataColorStop = {
    /** The minimum data value at which this color will be used */
    dataValue: number;
    /** The color to use */
    color: string;
};
export declare enum EColorPickMode {
    Discrete = "Discrete",
    Interpolated = "Interpolated"
}
export declare type THeightColorSettings = {
    /** The color to use for values less than the smallest stop */
    infraColor?: string;
    /** The color to use for values greater than the largest stop  */
    ultraColor?: string;
    /** The color stops to use in data space.  These must be in ascending order and there must be at least 2 stops, to define a minimum and maximum  */
    colorStops: TDataColorStop[];
    /**
     * If Interpolated, a color gradient will be created based on the stops, and colors will be picked from that gradient based on the relative position of the data value between the stops.
     * If Discrete, only the colors defined in the stops will be picked.
     * Default Discrete;
     */
    colorPickMode: EColorPickMode;
    /** Gradient mode blends the boundaries between neighboring segments, while Solid mode keeps them as pure colors. */
    fillMode?: EFillPaletteMode;
    /**  */
    maxTextureSize?: number;
};

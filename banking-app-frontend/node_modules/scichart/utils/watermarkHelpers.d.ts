import { Rect } from "../Core/Rect";
import { Size } from "../types/Size";
import { EWatermarkPosition } from "../types/WatermarkPosition";
declare function calcPosition(chartWidth: number, chartHeight: number, position: EWatermarkPosition, isRelativeToCanvas: boolean, seriesViewRectInput?: Rect, viewportSizeInput?: Size): {
    left: number;
    bottom: number;
};
export declare const watermarkHelpers: {
    calcPosition: typeof calcPosition;
};
export {};
